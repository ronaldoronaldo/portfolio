import styled from 'styled-components'
import { colors, shadows } from 'config/ui'
export const Dropdown = styled.div`
  width: 100%;
  background: ${colors.white};
  box-shadow: ${shadows.shadow8};
  border: 1px solid ${colors.black2};
  border-radius: 5px;
  max-width: 400px;
`
